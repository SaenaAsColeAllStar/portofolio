---
// HeroSystemMap.astro - Hydration wrapper for the Vue Hero System Map
import VueSystemMap from "./hero/HeroSystemMap.vue";

const locale = (Astro.currentLocale || 'en') as 'en' | 'id';
---
<VueSystemMap client:load locale={locale} />
