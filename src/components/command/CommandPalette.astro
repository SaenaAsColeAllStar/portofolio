---
// CommandPalette.astro - Hydration wrapper for the Vue Command Palette
import VuePalette from "./CommandPalette.vue";

const locale = (Astro.currentLocale || 'en') as 'en' | 'id';
---
<VuePalette client:idle locale={locale} />
