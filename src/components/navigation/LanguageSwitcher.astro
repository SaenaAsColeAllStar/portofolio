---
// LanguageSwitcher.astro - Language Switch component
const locale = (Astro.currentLocale || 'en') as 'en' | 'id';
const pathname = Astro.url.pathname;
let alternateUrl = pathname;
if (locale === 'en') {
	alternateUrl = `/id${pathname === '/' ? '' : pathname}`;
} else {
	alternateUrl = pathname.replace(/^\/id/, '') || '/';
}
---
<a href={alternateUrl} class="lang-switch-btn" transition:name="lang-badge" title={locale === 'en' ? "Terjemahkan ke Bahasa Indonesia" : "Translate to English"}>
	{locale === 'en' ? 'ID' : 'EN'}
</a>

<style>
	.lang-switch-btn {
		align-items: center;
		background: var(--btn-secondary-bg);
		border: 1px solid var(--border);
		border-radius: var(--radius-full);
		color: var(--muted);
		cursor: pointer;
		display: inline-flex;
		height: 2.2rem;
		justify-content: center;
		transition: transform 0.2s cubic-bezier(0.34, 1.56, 0.64, 1), color var(--duration-fast), background var(--duration-fast);
		width: 2.2rem;
		flex-shrink: 0;
		font-size: 0.78rem;
		font-weight: var(--font-weight-bold);
		text-decoration: none;
	}

	.lang-switch-btn:hover {
		transform: translateY(-2px);
		background: var(--btn-secondary-hover);
		color: var(--text);
	}

	.lang-switch-btn:active {
		transform: translateY(1px) scale(0.95);
	}
</style>
